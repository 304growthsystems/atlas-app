import Link from "next/link";import{notFound}from"next/navigation";import{ApplicationHeader}from"@/components/application-shell/application-header";import{PageContainer}from"@/components/application-shell/page-container";import{StatusControl}from"@/components/campaigns/management-controls";import{getCurrentContext}from"@/lib/auth";import{requireQueryData}from"@/lib/query";import{canManageFirstSlice}from"@/lib/security";
export default async function AdvertiserDetail({params}:{params:Promise<{advertiserId:string}>}){const{advertiserId}=await params;const{supabase,organizationId,membership}=await getCurrentContext();const[a,c,l,o,p,r]=await Promise.all([supabase.from('advertisers').select('*').eq('organization_id',organizationId).eq('id',advertiserId).maybeSingle(),supabase.from('advertiser_contacts').select('*').eq('organization_id',organizationId).eq('advertiser_id',advertiserId).eq('is_primary',true).maybeSingle(),supabase.from('advertiser_locations').select('*').eq('organization_id',organizationId).eq('advertiser_id',advertiserId).eq('is_primary',true).maybeSingle(),supabase.from('opportunities').select('id,stage,campaign_id').eq('organization_id',organizationId).eq('advertiser_id',advertiserId),supabase.from('placements').select('id,status,campaign_id,campaign_slot_id,sale_price_cents').eq('organization_id',organizationId).eq('advertiser_id',advertiserId),supabase.from('campaigns').select('id,name,status').eq('organization_id',organizationId)]);const advertiser=requireQueryData('advertiser detail',a);if(!advertiser)notFound();const contact=requireQueryData('advertiser contact',c),location=requireQueryData('advertiser location',l),opportunities=requireQueryData('advertiser opportunities',o),placements=requireQueryData('advertiser placements',p),campaigns=requireQueryData('advertiser campaigns',r);const campaignIds=new Set([...opportunities.map(x=>x.campaign_id),...placements.map(x=>x.campaign_id)]);const related=campaigns.filter(x=>campaignIds.has(x.id));const canManage=canManageFirstSlice(membership.role);return <><ApplicationHeader title={advertiser.name} eyebrow={advertiser.category} actions={canManage?[{label:'Edit advertiser',variant:'primary',href:`/advertisers/${advertiserId}/edit`}]:[]}/><PageContainer><section className="grid gap-4 rounded-2xl border bg-white p-5 sm:grid-cols-3"><Item label="Status" value={advertiser.status}/><Item label="Primary contact" value={contact?.name}/><Item label="Email" value={contact?.email}/><Item label="Phone" value={contact?.phone}/><Item label="Location" value={location?`${location.address_line_1}${location.address_line_2?`, ${location.address_line_2}`:''}, ${location.city}, ${location.state} ${location.postal_code}`:undefined}/><Item label="Created" value={new Date(advertiser.created_at).toLocaleDateString()}/><Item label="Updated" value={new Date(advertiser.updated_at).toLocaleDateString()}/></section>{canManage&&<StatusControl kind="advertiser" id={advertiserId} next={[advertiser.status==='active'?'archived':'active']}/>}<Related title="Opportunities" rows={opportunities.map(x=>`${x.stage.replaceAll('_',' ')}`)}/><Related title="Placements" rows={placements.map(x=>`${x.status.replaceAll('_',' ')} · ${x.sale_price_cents} cents`)}/><section><h2 className="text-xl font-bold">Related campaigns</h2>{related.length?<ul className="mt-3 space-y-2">{related.map(x=><li key={x.id}><Link className="text-red-700 underline" href={`/campaigns/${x.id}`}>{x.name}</Link> · {x.status.replaceAll('_',' ')}</li>)}</ul>:<p className="mt-2 text-sm text-slate-500">No related campaigns.</p>}</section><Related title="Current active reservations" rows={placements.filter(x=>['held','reserved','confirmed'].includes(x.status)).map(x=>`${x.status} · ${x.sale_price_cents} cents`)}/></PageContainer></>}
function Item({label,value}:{label:string;value?:string|null}){return <div><p className="text-xs text-slate-500">{label}</p><p className="font-semibold capitalize">{value||'—'}</p></div>}function Related({title,rows}:{title:string;rows:string[]}){return <section><h2 className="text-xl font-bold">{title}</h2>{rows.length?<ul className="mt-3 list-disc pl-5">{rows.map((x,i)=><li key={i} className="capitalize">{x}</li>)}</ul>:<p className="mt-2 text-sm text-slate-500">None.</p>}</section>}

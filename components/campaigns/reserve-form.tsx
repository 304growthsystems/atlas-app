import{reserveSlot}from"@/app/actions";import{ActionForm}from"@/components/forms/action-form";import{formatCurrency}from"@/lib/money";
export function ReserveForm({slotId,price,advertisers}:{campaignId?:string;slotId:string;price:number;advertisers:{id:string;name:string}[]}){return <ActionForm action={reserveSlot} submitLabel="Confirm reservation" className="mt-4 space-y-3"><input type="hidden" name="slotId" value={slotId}/><label className="grid gap-1 text-sm font-semibold">Advertiser<select required name="advertiserId" defaultValue="" className="rounded-lg border p-2 font-normal"><option value="" disabled>Select advertiser</option>{advertisers.map(a=><option key={a.id} value={a.id}>{a.name}</option>)}</select></label><label className="grid gap-1 text-sm font-semibold">Sale price ($)<input required name="salePrice" defaultValue={(price/100).toFixed(2)} className="rounded-lg border p-2 font-normal"/></label><p className="text-xs text-slate-500">Standard price: {formatCurrency(price)}. Submitting confirms this direct reservation.</p></ActionForm>}
